build:
    nodes:
        analysis:
            project_setup:
                override:
                    - 'true'
            tests:
                override:
                    - php ./vendor/bin/phpunit test --configuration phpunit.xml --testsuite scrutinizer

coding_style:
    php:
        indentation:
            general:
                use_tabs: true